# Overview

A project whose plan items are delegated to an external backlog, so each item's own status is a
cached copy and the `Tracked by` pointer is what must be resolved.

| Section | Covers |
| --- | --- |
| `01-work.md` | The delegated work items |
