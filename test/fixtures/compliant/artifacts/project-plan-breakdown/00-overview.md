# Overview

A small service exposing one API surface, planned in one section.

The overview carries prose on purpose: a file holding nothing but its heading is the shape
`standards init` leaves behind, and Standard 44 R11 says that is not a plan.

| Section | Covers |
| --- | --- |
| `01-api.md` | The HTTP surface and its handlers |
